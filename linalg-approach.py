import os
import sys
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt



'''
let's take a look at developing linear regression models using stochastic gradient decent,
batch gradient decent, and mini batch gradient decent

we'll generate a plot that shows the training error (RMSE) at every learning step the algorithm takes
find those in the plot folder!
'''